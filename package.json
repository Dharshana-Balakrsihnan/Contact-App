{
  "name": "contact-app",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run start-client\" \"npm run start-server\"",
    "start-client": "cd ./contact-hub && npm start",
    "start-server": "cd ./server-api && json-server -p 3006 -w db.json"
  },
  "dependencies": {
    "concurrently": "^6.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
